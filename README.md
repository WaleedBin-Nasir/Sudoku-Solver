# Sudoku-Solver
The CSP-based Sudoku solver implemented uses AC-3 arc consistency, forward checking, and backtracking with the Minimum Remaining Values (MRV) heuristic. Each board below shows the input puzzle, the solved state (where found), and the BACKTRACK call/failure statistics with commentary.
